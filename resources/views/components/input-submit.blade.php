<button
    class="btn btn-success"
    id="{{ $item['key'] }}"
    key="{{ $item['key'] }}"
    name="{{ $item['name'] }}"
    placeholder="{{ $item['placeholder'] }}"
    type="submit"
>
    {{ $item['placeholder'] }}
</button>
