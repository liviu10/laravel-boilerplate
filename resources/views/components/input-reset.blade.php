<button
    class="btn btn-secondary"
    id="{{ $item['key'] }}"
    key="{{ $item['key'] }}"
    name="{{ $item['name'] }}"
    placeholder="{{ $item['placeholder'] }}"
    type="reset"
>
    {{ $item['placeholder'] }}
</button>
